# 下一阶段研究与工程计划

## 当前基线

截至2026-07-11，CAD导入、等参数剖分、视点/路径、受约束TCP速度规划、CSV和RoboDK
逐点速度指令已集成。当前标志保持：

```text
dynamic_constraints_validated = False
```

它表示几何/TCP约束已检查，但机器人坐标链、连续IK和真实动力学尚未闭环。

## 主线：机器人可执行性闭环（必须顺序进行）

### A. 扫描仪到工具外参和坐标链 — IN PROGRESS / 最高优先级

目标：把“扫描视点姿态”明确转换为机器人实际命令的tool/flange姿态。

- [x] 定义并固定`T_world_scanner`、`T_tool_scanner`、`T_world_tool`方向和乘法约定。
- [x] 增加版本化JSON配置、单位、状态和4×4刚体矩阵验证。
- [x] UI加载/清除外参；未验证时UI和API双层阻止RoboDK导入。
- [x] CSV保存原始scanner pose、转换后的tool pose，元数据sidecar保存矩阵和SHA256。
- [x] 单位阵、纯平移、90°旋转、逆变换闭环和错误旋转测试。
- [ ] 用户提供真实`T_tool_scanner`、CAD world到RoboDK reference frame关系及标定报告。
- [ ] 在真实RoboDK站点/基准件上交叉验证多个位置姿态。

完成判据：同一组基准点可在Python矩阵测试和RoboDK站点中得到一致tool pose。当前软件
框架已完成，但缺少真实标定数据与站点验证，因此A尚未标记完成。

### B. 连续IK、关节限位和奇异性 — TODO / 依赖A

目标：为每个command pose选择连续且满足限位的UR10关节解。

- 获取全部IK候选，不只取第一个解。
- 用上一点关节角、周期角展开和加权关节距离选择连续分支。
- 检查机械限位、不可达点、分支跳变和腕部奇异区。
- 输出每点`q1..q6`、分支ID、最小限位余量、连续性代价和奇异性指标。
- 若无法稳定取得Jacobian，先使用可解释的腕部/数值差分指标并标记近似。

完成判据：测试路径不存在未报告的IK跳变；不可达/奇异点使整条轨迹不可生产导入。

### C. 真实关节速度和关节加速度约束 — TODO / 依赖B

目标：把当前TCP时间参数化与真实关节轨迹耦合。

- 根据连续`q(s)`计算关节一阶/二阶路径导数。
- 将每轴速度和加速度上限转成路径速度上限与前后向可达约束。
- 用非均匀时间步复核`dq/dt`、`d2q/dt2`，不能只用点值差。
- 输出每点/每段最大关节速度比和加速度比。
- Double-Q与确定性方法必须共享同一硬可行域。

完成判据：所有轴比值不超过1；数值复核与RoboDK生成程序的单位语义一致。

### D. 力矩、负载和控制器语义 — TODO / 依赖C及真实参数

目标：验证UR10加扫描仪/支架负载下的真实动力学可行性。

- 获取准确机器人型号、DH/动力学参数、扫描仪与支架质量、质心、惯量和安装姿态。
- 计算或调用可信模型得到逆动力学力矩，加入速度相关和重力项。
- 检查每轴连续/峰值力矩及安全因子。
- 检查目标控制器后处理器是否真实执行每段加速度设置。

阻塞条件：上述真实负载和控制器资料缺失时，只能保留接口与测试桩，不得伪造力矩结果。

完成判据：真实参数、计算方法、单位、限值来源和RoboDK/控制器验证报告全部可追溯，之后
才允许将`dynamic_constraints_validated`设为`True`。

## 支线：曲面剖分与测地线采样（可与主线并行研究）

### S1. 策略接口与自适应参数域剖分 — IN PROGRESS

- 已完成：`equal_param`逐split面积守恒、全局面积比、periodic/split/回退诊断。
- 已完成：带圆孔薄板回归测试；不守恒split保守回退到原face。
- TODO：把当前函数正式封装成策略接口。
- 新增`adaptive_complexity`，用法线变化、曲率和trim边界复杂度递归分块。
- 每个patch返回有效性、面积误差、失败原因和回退策略。

### S2. 网格图测地线近似 — TODO / 依赖S1

- 从BRep生成带容差记录的三角网格。
- 用Dijkstra距离作为可验证的测地线近似基线。
- 比较欧氏距离、图距离与已知曲面解析距离。

### S3. 论文热方法 — TODO / 依赖S2

- 构造cotangent Laplacian和质量矩阵。
- 解热方程、归一化热梯度及Poisson方程。
- 处理边界、非流形、退化三角形和多连通分量。
- 当前环境限制：Pytorch Conda有SciPy；集成GUI的test/Pythonocc组合没有直接SciPy。
  先做依赖隔离方案，不安装新包，不把不同ABI的NumPy/SciPy盲目混用。

### S4. 混合采样和测量质量评价 — TODO / 依赖S3

- 加入Poisson/Hammersley/NRook混合采样。
- 修复当前候选点距离相同导致的评价退化。
- 评价至少纳入入射角、覆盖、遮挡、曲率、路径连续性和可执行性。

## 每阶段共同交付要求

- 代码、最小单元测试、集成测试、配置示例、失败诊断和简明概要。
- 不安装依赖前先盘点现有Conda环境，避免base。
- 先在暂存副本验证，再同步最终Git目录；不自动提交。
- 未完成项保持TODO和`dynamic_constraints_validated=False`，不得用默认值伪装真实结果。
